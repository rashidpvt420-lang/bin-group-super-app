# CI Lint Follow-up 2

This note records the second PR-validation pass for the root lint correction.

## Real change being validated

The root ESLint configuration declares browser and Firebase runtime globals used by the app lint context.

## Expected checks

- PR Validation
- BIN GROUP CI
- Firebase Production Deploy after merge

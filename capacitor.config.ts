import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bingroup.app',
  appName: 'BIN Group Super App',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;

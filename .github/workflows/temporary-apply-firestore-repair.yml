name: Temporary Apply Firestore Repair

on:
  pull_request:
    branches:
      - main
    types: [opened]

permissions:
  contents: write

jobs:
  verify-repair:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Verification paused while fail-closed fallback contracts are aligned."

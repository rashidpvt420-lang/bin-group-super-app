name: RECOVERY - Admin MFA Enrollment (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Refuse parallel production deployment
    runs-on: ubuntu-latest
    steps:
      - name: Use canonical protected Admin MFA bootstrap
        run: |
          echo "::error::This parallel Admin MFA recovery deployment is retired. Dispatch Firebase Production Deploy in bank-pilot mode with incident_evidence_refs exactly ADMIN_MFA_BOOTSTRAP_HOSTING."
          exit 1

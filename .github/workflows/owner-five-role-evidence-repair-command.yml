name: Owner Five-Role Evidence Repair Command
run-name: Owner five-role evidence repair

on:
  issue_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: owner-five-role-evidence-repair
  cancel-in-progress: false

jobs:
  generate-repair:
    name: Generate deterministic five-role evidence repair PR
    if: >-
      github.event.issue.number == 434 &&
      !github.event.issue.pull_request &&
      github.event.comment.user.login == github.repository_owner &&
      github.event.comment.author_association == 'OWNER' &&
      github.event.comment.body == '/bin-repair five-role-production-evidence'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ github.token }}
      REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: '434'

    steps:
      - name: Resolve stable exact current main
        id: release
        shell: bash
        run: |
          set -euo pipefail
          first_sha="$(gh api "repos/$REPOSITORY/commits/main" --jq '.sha')"
          [[ "$first_sha" =~ ^[0-9a-f]{40}$ ]]
          sleep 3
          second_sha="$(gh api "repos/$REPOSITORY/commits/main" --jq '.sha')"
          [[ "$first_sha" == "$second_sha" ]] || {
            echo '::error::Main moved while authorizing the repair.'
            exit 1
          }
          echo "sha=$second_sha" >> "$GITHUB_OUTPUT"
          echo "short=${second_sha:0:8}" >> "$GITHUB_OUTPUT"

      - name: Checkout exact current main
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.release.outputs.sha }}
          fetch-depth: 0
          persist-credentials: true

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Apply exact fail-closed repair
        run: node scripts/apply-five-role-production-evidence-repair.mjs

      - name: Verify generated repair scope and regression
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          node --test tests/launch/five-role-production-evidence-repair.test.mjs
          test ! -e .github/workflows/owner-five-role-evidence-repair-command.yml
          test ! -e scripts/apply-five-role-production-evidence-repair.mjs
          mapfile -t changed < <(git diff --name-only | sort)
          expected=(
            .github/workflows/firebase-production-deploy.yml
            .github/workflows/owner-five-role-evidence-repair-command.yml
            scripts/apply-five-role-production-evidence-repair.mjs
            scripts/prepare-protected-business-fixtures.mjs
            src/technician/pages/TechnicianJobDetailPage.tsx
            tests/e2e/business-technician.spec.ts
            tests/e2e/business-tenant.spec.ts
            tests/launch/five-role-production-evidence-repair.test.mjs
          )
          mapfile -t expected_sorted < <(printf '%s\n' "${expected[@]}" | sort)
          diff -u <(printf '%s\n' "${expected_sorted[@]}") <(printf '%s\n' "${changed[@]}")

      - name: Commit repair to isolated branch
        id: commit
        env:
          RELEASE_SHA: ${{ steps.release.outputs.sha }}
          RELEASE_SHORT: ${{ steps.release.outputs.short }}
        shell: bash
        run: |
          set -euo pipefail
          latest_main="$(gh api "repos/$REPOSITORY/commits/main" --jq '.sha')"
          [[ "$latest_main" == "$RELEASE_SHA" ]] || {
            echo '::error::Main moved before repair branch creation.'
            exit 1
          }
          branch="fix/five-role-production-evidence-${RELEASE_SHORT}-generated"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git switch -C "$branch"
          git add -A
          git commit -m 'fix: make five-role production evidence deterministic'
          git push --force-with-lease origin "HEAD:refs/heads/$branch"
          echo "branch=$branch" >> "$GITHUB_OUTPUT"
          echo "head_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Open protected repair PR
        id: pr
        env:
          BRANCH: ${{ steps.commit.outputs.branch }}
          RELEASE_SHA: ${{ steps.release.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          body="$(cat <<EOF
          ## Verified failure
          Firebase Production Deploy run 30615219518 deployed the exact stack, then failed at current-commit five-role business evidence.

          ## Root causes fixed
          - Production evidence jobs did not receive the protected Founder login/TOTP secrets required by Owner inspection-first proof.
          - The shared protected fixture did not make the Technician operationally callable-ready before the Tenant cross-role lifecycle.
          - Technician evidence reused deterministic ticket IDs and assumed the transient ASSIGNED UI state.
          - Tenant evidence depended on one MUI chip instead of proving the Firestore lifecycle transition.
          - Online callable rejections were incorrectly described as offline queued success.

          ## Safety
          - Base exact main: \`$RELEASE_SHA\`
          - Public release gate remains disabled.
          - Hard-launch claim remains false.
          - Stripe and Bank Transfer remain disabled.
          - Temporary repair bootstrap removes itself in this PR.
          - A fresh exact-SHA deployment/evidence chain is required after merge.
          EOF
          )"
          pr_url="$(gh pr create --repo "$REPOSITORY" --base main --head "$BRANCH" --draft --title 'Fix five-role production evidence after successful Firebase deploy' --body "$body")"
          echo "url=$pr_url" >> "$GITHUB_OUTPUT"

      - name: Post repair handoff
        env:
          PR_URL: ${{ steps.pr.outputs.url }}
          HEAD_SHA: ${{ steps.commit.outputs.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          gh api --method POST "repos/$REPOSITORY/issues/$ISSUE_NUMBER/comments" \
            -f body="Five-role production evidence repair PR created: $PR_URL (head \`$HEAD_SHA\`). No deployment was retriggered."
